{
  "name": "web-portfolio-workspace",
  "private": true,
  "scripts": {
    "dev": "cd web-portfolio && npm run dev",
    "build": "cd web-portfolio && npm run build",
    "start": "cd web-portfolio && npm run start",
    "lint": "cd web-portfolio && npm run lint"
  }
}
